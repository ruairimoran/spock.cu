// AUTOGENERATED FILE \~timestamp~\

#pragma once
#include <vector>
using namespace std;

// ---- Scenario tree

// Type
bool const markovian = \% if is_markovian %\true\% else %\false\% endif %\;

// Attributes
const int num_nonleaf_nodes = \~num_nonleaf_nodes~\;
const int num_nodes = \~num_nodes~\;
const int num_stages = \~num_stages~\;
const int stages[\~num_nodes~\] = {\% for i in stages %\\~i~\,\% endfor %\};
const int ancestors[\~num_nodes~\] = {\% for i in ancestors %\\~i~\,\% endfor %\};
const double probability[\~num_nodes~\] = {\% for i in probability %\\~i~\,\% endfor %\};
const int events[\~num_nodes~\] = {\% for i in events %\\~i~\,\% endfor %\};
\% for i in range(num_nonleaf_nodes) %\
vector<const int> ch_of_\~i~\{\% for j in children[i] %\\~j~\,\% endfor %\};
\% endfor %\
\# int const children[\~num_nonleaf_nodes~\] = {\% for i in children %\\~i[-1]~\,\% endfor %\}; #\
vector<const int> children[\~num_nonleaf_nodes~\] = {\% for i in range(num_nonleaf_nodes) %\ch_of_\~i~\,\% endfor %\};
 