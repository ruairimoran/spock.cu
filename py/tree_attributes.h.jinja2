// AUTOGENERATED FILE \~timestamp~\

// ---- Scenario tree

// -- Type
// is tree generated by a stopped Markov process?
bool const _is_markovian = \% if is_markovian %\true\% else %\false\% endif %\;
// is tree generated by an independent and identically distributed sequence?
bool const _is_iid = \% if is_iid %\true\% else %\false\% endif %\;

// -- Attributes
// total number of nonleaf nodes
const int _num_nonleaf_nodes = \~num_nonleaf_nodes~\;
// total number of nodes
const int _num_nodes = \~num_nodes~\;
// total number of stages (N+1)
const int _num_stages = \~num_stages~\;
// array of stage of node at index
const int stages[\~num_nodes~\] = {\% for i in stages %\\~i~\,\% endfor %\};
// array of ancestor of node at index
const int ancestors[\~num_nodes~\] = {\% for i in ancestors %\\~i~\,\% endfor %\};
// array of probability of visiting node at index
const double probability[\~num_nodes~\] = {\% for i in probability %\\~i~\,\% endfor %\};
// array of event occurred that led to node at index
const int events[\~num_nodes~\] = {\% for i in events %\\~i~\,\% endfor %\};
\% for i in range(num_nonleaf_nodes) %\
// vector of children of node \~i~\
vector<const int> ch_of_\~i~\{\% for j in children[i] %\\~j~\,\% endfor %\};
\% endfor %\
// array of pointer-to-vectors of children of node at index
vector<const int> children[\~num_nonleaf_nodes~\] = {\% for i in range(num_nonleaf_nodes) %\ch_of_\~i~\,\% endfor %\};
 