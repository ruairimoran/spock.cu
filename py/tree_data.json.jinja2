{
\# AUTOGENERATED FILE \~timestamp~\ #\
\# ---- Scenario tree #\
\# -- Type #\
\# is tree generated by a stopped Markov process? #\
"is_markovian": \% if is_markovian %\true\% else %\false\% endif %\,
\# is tree generated by an independent and identically distributed sequence? #\
"is_iid": \% if is_iid %\true\% else %\false\% endif %\,
\# -- Attributes #\
\# total number of nonleaf nodes #\
"num_nonleaf_nodes": \~num_nonleaf_nodes~\,
\# total number of nodes #\
"num_nodes": \~num_nodes~\,
\# total number of stages (N+1) #\
"num_stages": \~num_stages~\,
\# array of stage of node at index #\
"stages": [\% for i in stages[0:-1] %\\~i~\,\% endfor %\\~stages[-1]~\],
\# array of ancestor of node at index #\
"ancestors": [\% for i in ancestors[0:-1] %\\~i~\,\% endfor %\\~ancestors[-1]~\],
\# array of probability of visiting node at index #\
"probabilities": [\% for i in probabilities[0:-1] %\\~i~\,\% endfor %\\~probabilities[-1]~\],
\# array of event occurred that led to node at index #\
"events": [\% for i in events[0:-1] %\\~i~\,\% endfor %\\~events[-1]~\],
\# array of first child of node at index #\
"children_from": [\% for i in children[0:-1] %\\~i[0]~\,\% endfor %\\~children[-1][0]~\],
\# array of last child of node at index #\
"children_to": [\% for i in children[0:-1] %\\~i[-1]~\,\% endfor %\\~children[-1][-1]~\]
}